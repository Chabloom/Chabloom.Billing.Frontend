export interface UpdateViewModel {
  readonly id: string;
  name: string;
  amount: number;
  currency: string;
  dueDate: string;
  complete: string;
  transaction: string;
  transactionSchedule: string;
}
