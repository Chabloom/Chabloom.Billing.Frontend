apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Chabloom.Billing.Frontend.fullname" . }}-test-connection"
  labels:
    {{- include "Chabloom.Billing.Frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Chabloom.Billing.Frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
